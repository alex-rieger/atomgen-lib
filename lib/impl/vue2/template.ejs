<template>
  <%- template %>
</template>

<script>
  export default {
    name: "<%= component.name.internal %>",
    props: {<% getPropsList(component.props).forEach(function(prop){ %>
        "<%=prop.name%>": {
          type: <%= propTypeToConstructor(prop.type) %>,
          required: <%= prop.required %>,
          default: <%- propDefaultToValue(prop.default) %>,
        },<% }) %>
    },
  }
</script>


